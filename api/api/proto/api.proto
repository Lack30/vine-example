syntax = "proto3";

import "github.com/lack-io/vine/proto/api/api.proto";

service Example {
  rpc Call(api.Request) returns(api.Response) {};
}

service Foo {
  rpc Bar(api.Request) returns(api.Response) {};
}